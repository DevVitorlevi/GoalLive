import React from 'react';
import { HeaderContainer, Logo } from '../styles/header'

export const Header = () => {
    return (
        <HeaderContainer>
            <Logo>GOALLIVE</Logo>
        </HeaderContainer>
    );
};